# Core dependencies for semantic search
torch>=2.0.0
transformers>=4.36.0
huggingface-hub>=0.19.0
faiss-cpu>=1.7.4  # Use faiss-gpu if CUDA is available
numpy>=1.24.0
sentence-transformers>=2.2.0  # For additional utilities
watchdog>=3.0.0  # For file change monitoring

# Optional but recommended
accelerate>=0.25.0  # For faster model loading
safetensors>=0.4.0  # For safer model weight loading